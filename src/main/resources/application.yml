server:
  port: 18081

agentscope:
  model:
    api-key: ${DASHSCOPE_API_KEY}
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    model-name: qwen3.5-122b-a10b
    stream: true
  react:
    max-iters: 30
    max-actions-per-call: 5

memory:
    session-dir: ./memory/sessions
data:
  conversation-dir: ./data/conversations
prompt:
  locale: /prompt
agent-config:
  prompt-dir: ./data/agent-config/prompts

file:
  base: /temp/my-manus-agentscope
  domain: http://localhost:7365